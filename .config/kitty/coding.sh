# Create a new tab for Neovim
new_tab Neovim
launch nvim .

# Create a new tab for Server/other processes
new_tab Server
layout grid
# You can add other commands here for your "Server" tab, e.g.:
# launch zsh -c "npm run dev"
# launch zsh -c "tail -f logs/server.log"
